# 계약 현황

프론트가 의존하는 크로스파트 계약이 **어디까지 확정됐고 무엇을 기다리는지** 한곳에 모은 문서다.

이 문서의 목적은 다음 작업자와 에이전트가 원본 문서를 다시 읽지 않고도
무엇이 정해졌는지 알게 하는 것이다. **규약의 본문은 여기가 아니라 원본에 있다.**
값이 어긋나면 원본이 진실이다.

- 기준일: 2026-09-03 (**apiSpec v0.7 — 투자 회차·계좌 리셋 제거**. 우리 요청(이슈 #27)에 대한 백엔드 회신이고 **기존 계약을 깨는 변경이다**. 엔드포인트 2개(`POST /account/reset`·`GET /rounds`) 삭제 · `roundId`·`currentRoundId` 필드와 `roundId` 쿼리 전부 삭제 · `ROUND_READ_ONLY` 삭제 · 원장 유형 6종→4종 · 충전 누적 한도 기준이 회차→계정 전체로 바뀌고 `GET /deposits/limit` 응답 필드명도 함께 바뀜(→ C26·C47·C49 갱신, C48 폐기, P22 신규). **근거 MR !78 은 아직 열려 있다** — 아래 주의 참고. 이전 기준일 2026-09-02)
- 이전 기준일 2026-09-02 (백엔드가 열려 있던 이슈 5건 — #10·#11·#12·#19·#22 — 에 한꺼번에 회신. AI 재포장 후 `content` 컨테이너 유지 확정(이슈 #22, 프론트가 반대 가정으로 구현했던 것이 뒤집힘) · 웹소켓 CONNECT 인증 실패·토큰 만료·SockJS 여부 확정(이슈 #10) · AI 응답 본문의 `ticker` 필드 이름 유지 확정(이슈 #11) · AI 중계 요청 본문 camelCase 확정(이슈 #12) · 전량 매도·상장폐지 종목 노출 규칙 확정(이슈 #19) · 이슈 #13 11:02 회신으로 `wiki`·`feedback` 전용 pydantic 모델 반영 확인 · 새 이슈 #23(최근 검색어) 발행 · GitLab 이슈 상태 재확인. 이슈 #23 회신과 apiSpec v0.6 §6.2 로 최근 검색어 응답 본문 확정(→ C81) · `profileImageUrl` 이 `null` 일 수 있다는 계약을 apiSpec v0.6 §2.1·§2.4 와 MR !69 근거로 신규 등재(→ C82). 이전 기준일 2026-09-01)
- 참조 원본
  - [`docs/api/apiSpec.md`](../../docs/api/apiSpec.md) — 백엔드 API 명세 **v0.7 (확정판).**
    이 문서가 **단일 기준**이다. 절 번호 표기 `§`는 별도 표시가 없으면 이 문서를 가리킨다
  - [`docs/spec/featureSpec.md`](../../docs/spec/featureSpec.md) — 기능 명세서 v2.2
  - [`ai/docs/api-spec.md`](../../ai/docs/api-spec.md) — AI 파트 인터페이스 계약. **AI 계열은 이 파일이 기준이다**
  - [`ai/docs/openapi.json`](../../ai/docs/openapi.json) — AI 서버의 실제 구현 스키마.
    AI 파트 문서와 어긋나면 구현이 맞고 문서가 틀린 것이다
  - GitLab 이슈 [#1](https://lab.ssafy.com/s15-fintech-finance-sub1/S15P21A101/-/issues/1) 회신 (19문항, 닫힘)
- 관련 규약: [`docs/convention/frontConvention.md`](../../docs/convention/frontConvention.md) (크로스파트) ·
  [`frontConvention.md`](./frontConvention.md) (프론트 내부) · [`ia.md`](./ia.md) (화면 목록)

> **⚠️ v0.7 의 근거 MR !78 은 아직 `master` 에 머지되지 않았다.**
> 이 문서는 **v0.7 기준으로 갱신했다** — 프론트가 뒤늦게 나가면 목과 실서버가 갈리기 때문이다.
> 다만 `master` 의 `docs/api/apiSpec.md` 는 아직 v0.6 이므로 링크를 따라가면 v0.6 이 보인다.
> v0.7 원문은 MR !78 의 소스 브랜치(`S15P21A101-126-investment_round-account-v2`)에 있다.
> **MR !78 이 리뷰에서 뒤집히면 이 판의 C26·C47·C49·P22 와 C48 폐기를 함께 되돌린다.**

> **공통 API 규격 v0.3은 기준에서 폐기됐다.** 값이 같아도 근거를 v0.3으로 적으면
> 다음 사람이 폐기된 문서를 다시 찾아 읽는다. 이 문서의 근거 열은 전부 `apiSpec.md` 기준으로 바꿨다.

**이 문서는 확정 / 잠정 확정 / 미확정 / 충돌의 네 단계를 쓴다.**
잠정 확정은 회신이 없어 프론트가 임의로 정하고 진행하는 것이다. 확정이 아니다.
**팀 회신이 오면 그 값이 우선하고 잠정 확정 항목은 폐기한다.**

---

## 1. 확정

### 1.1 호출 경로와 응답 형식

| #   | 항목                                    | 확정된 값                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 출처                                           |
| --- | --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------- |
| C1  | 프론트 Base URL                         | **`/api/v1` 하나.** AI 기능도 `/api/v1/ai/**`로 부른다. 프론트는 AI 서버(`/api/ai/v1`)를 직접 호출하지 않는다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | §1.1 · §10.1                                   |
| C2  | AI 호출 경로                            | **프런트 → 백엔드 → AI.** 백엔드가 JWT를 검증하고, AI 서버는 사용자 식별을 백엔드의 신뢰 헤더로 받는다. AI 서버는 외부에 직접 노출되지 않는다                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | §10 · §10.2                                    |
| C3  | AI 중계 경로 7종                        | `POST /ai/stocks/{stockCode}/analysis` · `POST /ai/chat` · `POST /ai/portfolio/diagnosis` · `POST /ai/portfolio/attribution` · `POST /ai/orders/preview` · `GET /ai/briefing` · `POST /ai/feedback`. **`briefing`만 GET이다**                                                                                                                                                                                                                                                                                                                                                                                      | §10.1 · §12 · 이슈 #1 ⑤                        |
| C4  | AI 전송 방식                            | **전부 단발 요청/응답. SSE는 폐기됐다.** `shared/api`에 스트림 파서를 만들지 않는다. 되살리려면 백엔드 스트리밍 프록시가 먼저다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | AI 명세 §2.5 · §10(v0.5) · 이슈 #1 ⑯           |
| C5  | AI 기능 범위                            | **사용자 대면 6종 + `feedback` 1종 + 위키 3종.** 위키 3종(`GET /wiki` · `PUT /wiki/theses/{stockCode}` · `DELETE /wiki/facts/{factId}`)의 중계 경로가 §10.1 표에 정식으로 올라갔다(C80). `POST /wiki/theses`는 AI 서비스가 스스로 부르므로 프론트가 부르지 않는다                                                                                                                                                                                                                                                                                                                                                  | §10.1(v0.5) · featureSpec §10.2 · 이슈 #7 회신 |
| C6  | 성공 응답 형식                          | 백엔드는 **봉투 없이 리소스 본문**을 내려준다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | §1.3                                           |
| C7  | AI 성공 응답 재포장                     | 백엔드가 AI 봉투를 벗기고 **백엔드 형식(camelCase)으로 재포장**한다. 나머지 봉투 필드(`generated_at` · `model` · `cached`)는 걷어내고, **`content` 컨테이너와 보존 필드 4종(`dataAsOf` · `citations` · `disclaimer` · `requestId`)만 본문에 남는다** — 최종 모양은 `{content: {...}, dataAsOf, citations, disclaimer, requestId}`다. `content` 내부 키는 snake_case → camelCase 변환만 적용된다(종목코드 필드 이름은 바뀌지 않는다, C74). **이전 판은 이 항목을 (가) 평탄화(`content` 키가 최상위로 올라옴)로 가정하고 구현했으나, 이슈 #22 회신으로 (나) `content` 유지로 뒤집혔다.** 되돌리는 작업이 진행 중이다 | §10.3 · 이슈 #1 ⑨ · 이슈 #10 ⑤ · 이슈 #22      |
| C8  | 에러 응답 형식                          | **`{code, message, detail}`.** `isSuccess`는 두지 않는다. 검증 실패 시 `detail`이 `{필드명: 사유}` 맵이다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | §1.3 · 이슈 #1 ⑧                               |
| C9  | 성공·실패 판단                          | **성공 여부는 HTTP 상태 코드로, 실패 종류는 `code` 문자열로만** 분기한다. 상태 코드 숫자나 `message` 문구로 실패 종류를 가르지 않는다. 배포된 `code`는 변경되지 않는다                                                                                                                                                                                                                                                                                                                                                                                                                                             | §1.3                                           |
| C10 | 에러 문구                               | `message`를 사용자에게 그대로 노출한다. 화면마다 문구를 다시 만들지 않는다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | §1.3                                           |
| C11 | AI 중계 에러                            | AI 서버의 `code` · `message` · `detail`과 **HTTP 상태를 그대로 통과**시킨다. 백엔드가 자기 5xx로 뭉개지 않는다. 백엔드가 AI 서버에 도달하지 못한 경우만 `AI_UPSTREAM_UNAVAILABLE`(502) · `AI_UPSTREAM_TIMEOUT`(504)을 새로 발행한다                                                                                                                                                                                                                                                                                                                                                                                | §10.4 · §11 · 이슈 #1 ⑦                        |
| C12 | `INSUFFICIENT_DATA`                     | **에러가 아니라 정상적인 거절이다.** 보유 종목 0개나 가격 히스토리 부족에서 나온다. **AI 영역만 대체 문구로 접고 화면 전체를 실패로 만들지 않는다.** 사유는 `detail.reason`으로 분기한다                                                                                                                                                                                                                                                                                                                                                                                                                           | AI 명세 §2.6                                   |
| C13 | 429 처리                                | AI 계열에만 `RATE_LIMITED`(429)가 정의돼 있고 `Retry-After` 헤더 값만큼 대기한다. **백엔드 에러 코드 목록에는 429가 없다**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | AI 명세 §2.6 · §11                             |
| C14 | 요청 추적                               | 모든 응답에 `X-Request-Id` 헤더가 실린다. **AI 중계 경로는 본문 최상위에 `requestId`가 더 실리고 에러 응답에도 보존된다** — `POST /ai/feedback`이 이 값으로 원본 응답을 찾는다. 백엔드 자체 에러에는 이 필드가 없다                                                                                                                                                                                                                                                                                                                                                                                                | §1.1 · §1.3 · §10.3 · 이슈 #1 ⑭                |
| C69 | 엔드포인트별 에러 `code` 전체 목록      | apiSpec v0.4가 §11.1(공통 계층 3그룹: 전체·인증 필요·멱등성 필수)·§11.2(엔드포인트별 고유 코드와 판정 순서) 표를 신설했다. **표에 없는 코드는 그 엔드포인트에서 나오지 않는다.** 값 자체는 확정이지만, GitLab 이슈 [#4](https://lab.ssafy.com/s15-fintech-finance-sub1/S15P21A101/-/issues/4)는 **열려 있다** — 완료 판정이 "0-6 OpenAPI 산출물에 포함"인데 이번에 갱신된 것은 `apiSpec.md` 문서이지 OpenAPI 산출물이 아니다                                                                                                                                                                                       | §11.1 · §11.2(v0.4, 커밋 `ac76a7c`)            |
| C70 | `AI_UPSTREAM_*` 에러의 `requestId` 유무 | `AI_UPSTREAM_UNAVAILABLE`·`AI_UPSTREAM_TIMEOUT`은 **백엔드 자체 에러라 `requestId`가 없다.** AI 서버가 응답한 적이 없어 `POST /ai/feedback`으로 찾을 원본이 없다. C14의 "에러 응답에도 보존"은 AI 서버가 돌려준 나머지 코드에만 해당한다. 프론트는 이 두 코드 자리에 피드백 슬롯을 만들지 않는다                                                                                                                                                                                                                                                                                                                   | §10.4(v0.4, 커밋 `ac76a7c`) · 이슈 #12 ④       |
| C75 | AI 중계 요청 본문의 키 표기             | 프론트가 백엔드로 보내는 **요청** 본문도 `requestId`\·`linkedTradeId`처럼 **camelCase**다. AI 서버로 넘길 때의 snake_case 변환은 백엔드 중계 레이어가 담당한다. 응답 재포장(C7)과 대칭이라 **프론트는 요청·응답 양방향 모두 camelCase 하나만 쓴다**(구 P15)                                                                                                                                                                                                                                                                                                                                                        | §10.3(v0.5) · 이슈 #12 ③                       |

### 1.2 단위와 표기

| #   | 항목                                 | 확정된 값                                                                                                                                                                                                                                                                                                                                                                                                                                            | 출처                     |
| --- | ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ |
| C15 | 필드 네이밍                          | 백엔드 `camelCase`. AI 중계분도 재포장으로 `camelCase`가 된다(C7)                                                                                                                                                                                                                                                                                                                                                                                    | §1.1 · §10.3             |
| C16 | 시각 표기                            | ISO 8601 + KST 오프셋 명시 (`2026-08-20T14:30:00+09:00`)                                                                                                                                                                                                                                                                                                                                                                                             | §1.1                     |
| C17 | 금액과 수량                          | 원 단위 정수(`long`). 소수점·콤마 없이 숫자만 온다. 천 단위 콤마는 화면에서 처리한다                                                                                                                                                                                                                                                                                                                                                                 | §1.1                     |
| C18 | 등락률·수익률                        | **백분율 값.** `-1.21`은 −1.21%다. **프론트는 `%` 기호만 붙이고 100을 곱하지 않는다.** 그 밖의 비율(비중 등)만 0~1 소수다                                                                                                                                                                                                                                                                                                                            | §1.1 · 이슈 #1 ①         |
| C19 | 종목코드                             | 6자리 **문자열**. 정수 금지(앞자리 0 소실). 프론트 파라미터 이름은 **`stockCode`로 통일**                                                                                                                                                                                                                                                                                                                                                            | §1.1 · §12               |
| C74 | AI 응답 본문 안의 종목코드 필드 이름 | 경로 파라미터는 `{stockCode}`(C19)로 통일되지만, **AI 응답 본문 안의 `ticker` 계열 필드는 이름이 바뀌지 않고 그대로 나간다.** 표기만 camelCase 변환(`related_tickers` → `relatedTickers`). `content.ticker`·`findings[].evidence.tickers`·`contributors[].ticker`·`detractors[].ticker`·`order_summary[].ticker`·`thesis_conflicts[].ticker`·`items[].related_tickers` 5곳 전부 이 규칙이고, 엔드포인트별 `ticker`→`stockCode` 매핑 표는 두지 않는다 | §10.3(v0.5) · 이슈 #11 ① |

### 1.3 인증과 공통 규약

| #   | 항목                        | 확정된 값                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 출처                                       |
| --- | --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------ |
| C20 | 인증 수단                   | **카카오 OAuth 2.0 단일.** 자체 회원가입 폼 없음. 서비스 인증은 JWT                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | featureSpec §2.1 · §2.1                    |
| C21 | Access Token                | 유효 30분. 응답 **본문** 전달, 프론트 **메모리에만** 보관 (`localStorage` 금지)                                                                                                                                                                                                                                                                                                                                                                                                                                                      | §1.2                                       |
| C22 | Refresh Token               | 유효 14일. **`HttpOnly + Secure + SameSite=Lax; Path=/api/v1/auth; Max-Age=1209600` 쿠키 전용.** 응답 본문에 `refreshToken`이 없고 JS에서 접근할 수 없다                                                                                                                                                                                                                                                                                                                                                                             | §1.2 · §2.1 · 이슈 #1 ③                    |
| C23 | 인증 인터셉터 분기 기준     | **`code` 문자열로만 분기한다. HTTP 상태 숫자로 판단하지 않는다.** `AUTH_TOKEN_EXPIRED`에서만 `POST /auth/refresh` 후 원요청을 재시도하고, `AUTH_INVALID_TOKEN`(변조·무효)은 재시도 없이 로그인 화면으로 보낸다. **그 밖의 코드는 세션을 건드리지 않고 그대로 넘긴다.** 상세는 [`frontConvention.md`](./frontConvention.md) §5 참고                                                                                                                                                                                                   | §1.2 · §11 · 이슈 #1 ⑮                     |
| C24 | 부팅 시 토큰 복구           | `POST /auth/refresh` 1회, **요청 본문 없음**(쿠키로 전달). 쿠키가 없는 최초 방문자는 `AUTH_REFRESH_TOKEN_MISSING`으로 구분된다                                                                                                                                                                                                                                                                                                                                                                                                       | §2.2 · 이슈 #1 ④                           |
| C25 | 사용자 식별                 | 토큰에서만 추출한다. 경로·본문·쿼리로 `userId`를 받지 않는다                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | §10.2 · AI 명세 §2.1                       |
| C26 | 계좌 규칙                   | 계좌는 **사용자당 하나**이고 계정 생성과 함께 만들어진다. 모든 요청은 토큰의 사용자로 계좌를 찾는다 — **요청에 계좌 식별자를 받지 않고 응답에도 내려주지 않는다.** 계좌 도메인이 붙어도 `accountId` 로 되살리지 않는다. **투자 회차와 계좌 리셋은 v0.7 에서 없어졌다**(이슈 #27) — 원장은 계정 생성부터 이어지는 하나의 연속된 시계열이다. `roundId`·`currentRoundId` 필드, `roundId` 쿼리, `ROUND_READ_ONLY` 코드, 원장 유형 `ROUND_OPEN`·`ROUND_CLOSE` 가 전부 삭제됐고 엔드포인트 `POST /account/reset`·`GET /rounds` 도 사라졌다 | §1.6(v0.7) · §2.4 · §3 · 이슈 #27 · MR !78 |
| C27 | 커서 페이징                 | `size` 기본 **30**, 최대 100. 응답 `{items, nextCursor, hasNext}`. `nextCursor`가 `null`이면 마지막 페이지다                                                                                                                                                                                                                                                                                                                                                                                                                         | §1.5 · 이슈 #1 ⑲                           |
| C28 | 커서 성격                   | **불투명 문자열.** 파싱·조작·해석하지 않고 `nextCursor`를 그대로 되돌려 보낸다                                                                                                                                                                                                                                                                                                                                                                                                                                                       | §1.5                                       |
| C29 | 멱등성 적용 대상            | `POST /deposits`와 `POST /orders`에 `Idempotency-Key` **필수**. 누락은 `400 IDEMPOTENCY_KEY_REQUIRED`                                                                                                                                                                                                                                                                                                                                                                                                                                | §1.4 · §12                                 |
| C30 | 멱등성 키 생성              | **클라이언트가 UUID v4 생성.** 같은 버튼 클릭의 재시도는 같은 키, 새 클릭은 새 키. 서버 보관 기간 24시간                                                                                                                                                                                                                                                                                                                                                                                                                             | §1.4                                       |
| C31 | 멱등성 충돌 처리            | `409 IDEMPOTENCY_IN_PROGRESS` → 짧게 대기 후 **동일 키로** 재시도. `409 IDEMPOTENCY_CONFLICT` → 프론트 버그 신호, **재시도 금지**                                                                                                                                                                                                                                                                                                                                                                                                    | §1.4                                       |
| C32 | 로컬 개발 오리진            | Vite dev proxy로 `/api`를 백엔드에 프록시해 동일 오리진화한다. 로컬 CORS·쿠키 도메인 설정이 필요 없다                                                                                                                                                                                                                                                                                                                                                                                                                                | §1.1 · §1.2 (쿠키 Path)                    |
| C82 | `profileImageUrl` 의 `null` | **`null` 이 정상 경로다.** 카카오 프로필 사진은 **선택 동의 항목**이라 사용자가 동의하지 않으면 값이 없다. `GET /users/me`·`POST /auth/kakao` 응답 모두 해당한다. **빈 문자열이 아니라 `null` 이다** — `url === ''` 로 분기하면 조용히 틀린다. Zod 는 `.optional()` 이 아니라 `.nullable()` 로 짠다(C54 — 선언된 키는 값이 없어도 빠지지 않는다). 프론트는 기본 아바타로 대체한다                                                                                                                                                    | §2.1 · §2.4(v0.6 명시) · MR !69            |

### 1.4 시세

| #   | 항목                          | 확정된 값                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 출처                    |
| --- | ----------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- |
| C33 | 실시간 시세 전달 방식         | **웹소켓(STOMP) + REST 폴링 폴백.** 두 경로의 시세 페이로드 스키마는 동일하다                                                                                                                                                                                                                                                                                                                                                                                                                            | §5.6                    |
| C34 | 프론트 폴링 선행 구현         | **승인됨.** 폴링은 임시 코드가 아니라 최종 구조의 폴백 경로다. 단 `subscribe(stockCode, cb)` / `unsubscribe(stockCode)` 추상화 뒤에 숨기고 그 뒤에서 STOMP 클라이언트로 교체한다                                                                                                                                                                                                                                                                                                                         | §5.6                    |
| C35 | KIS 한도와 구독 범위          | **KIS 등록 한도는 프론트 계약에 노출되지 않는다.** 백엔드 수집 계층이 흡수한다 — 관심도 상위 종목만 KIS 실시간에 등록하고 나머지는 백엔드가 KIS REST를 폴링해 하나의 시세 캐시에 동일 스키마로 적재한다. **프론트는 종목이 어느 티어인지 알 수 없고 신선도 차이는 `asOf`로만 드러난다.** 화면에 보이는 종목만 구독하고 관심 종목을 상시 구독하지 않는다는 규칙은 유지하되 **이유가 바뀌었다** — 슬롯 한도를 프론트가 관리하기 때문이 아니라, 아무도 보지 않는 구독과 폴링 요청을 만들지 않기 위한 것이다 | §5.6                    |
| C36 | 슬롯 회수 주체                | **서버.** 종목별 마지막 관심 시각(STOMP 구독 존재 여부 또는 폴링 요청 시각) 기준으로 서버 전체 LRU로 배정·회수한다. **클라이언트의 해제 신호에 의존하지 않는 구조이므로 프론트는 `pagehide`·`visibilitychange` 등 이탈 이벤트에서 별도 신호를 보내지 않는다**                                                                                                                                                                                                                                            | §5.6                    |
| C37 | 구독·해제 신호 채널           | **웹소켓은 STOMP 프레임 자체가 신호다** — `/topic/prices/{stockCode}` SUBSCRIBE가 구독, UNSUBSCRIBE 또는 연결 종료가 해제다. 별도 구독 메시지도 REST 신호 엔드포인트도 없다. **폴링은 무신호** — `GET /stocks/prices` 요청 자체가 관심 신호이고 TTL 경과가 해제다. 추상화 계층에서 `subscribe()`는 폴링 시작, `unsubscribe()`는 폴링 중단이다                                                                                                                                                            | §5.6 · 이슈 #6          |
| C38 | 웹소켓 인증                   | 핸드셰이크(`WS /ws`)는 **인증 없이 통과**시킨다. 인증은 **CONNECT 프레임의 `Authorization: Bearer {accessToken}` 헤더**에서 검증한다 — 브라우저 `WebSocket` 생성자가 커스텀 헤더를 붙일 수 없기 때문이다. **URL 쿼리로 토큰을 보내지 않는다**(로그 노출)                                                                                                                                                                                                                                                 | §5.6 · 이슈 #1 ⑬        |
| C71 | 웹소켓 CONNECT 인증 실패 응답 | **STOMP ERROR 프레임**을 보내고 서버가 연결을 닫는다. `code`는 REST와 같은 체계를 재사용한다 — 토큰 만료는 `AUTH_TOKEN_EXPIRED`, 헤더 누락·변조·무효는 `AUTH_INVALID_TOKEN`. ERROR 프레임의 `message` 헤더에 이 `code` 문자열만 실리고, **§1.3의 JSON 에러 본문 형식은 따르지 않는다.** 분기는 REST 인터셉터와 동일 — `AUTH_TOKEN_EXPIRED`는 재발급 후 재CONNECT, `AUTH_INVALID_TOKEN`은 로그인 화면(구 P1)                                                                                              | §5.6(v0.5) · 이슈 #10 ② |
| C72 | 웹소켓 연결 유지 중 토큰 만료 | **끊지 않는다.** 토큰 검증은 CONNECT 시 1회뿐이고, 연결 유지 중 Access Token이 만료돼도 세션은 유지된다. 시세 topic은 사용자별 데이터가 아니라 공개 시세라 노출 위험이 없다는 것이 이유다. 네트워크 단절로 재연결할 때는 CONNECT 재검증에서 만료가 걸려 C71 규칙대로 처리된다(구 P1)                                                                                                                                                                                                                     | §5.6(v0.5) · 이슈 #10 ③ |
| C73 | `WS /ws`의 프로토콜           | **순수 WebSocket 단일.** SockJS 폴백은 두지 않는다. WS 자체가 안 되는 환경은 REST 폴링 폴백(C33)이 흡수한다. 접속은 `ws://` 스킴 그대로, `sockjs-client`를 쓰지 않고 STOMP 클라이언트만 붙인다(구 P1)                                                                                                                                                                                                                                                                                                    | §5.6(v0.5) · 이슈 #10 ④ |
| C39 | 하트비트                      | STOMP 내장 하트비트 **10초/10초 양방향**. **3회 미수신(30초)** 시 서버가 연결을 닫고 슬롯을 회수한다. 정상 종료는 disconnect 이벤트로 즉시 회수                                                                                                                                                                                                                                                                                                                                                          | §5.6 · 이슈 #1 ⑪        |
| C40 | 폴링 주기                     | **계약이 아니다.** 서버가 보장하는 것은 **폴링 티어 TTL 30초**뿐이고 주기는 프론트 재량이다. 권장값은 목록 화면 5초 · 주문 화면 3초이며, 관계식(**TTL ≥ 주기 × 4~6**)만 지키면 자유롭게 조정할 수 있고 조정 시 명세를 고칠 필요가 없다                                                                                                                                                                                                                                                                   | §5.6 · 이슈 #1 ⑪        |
| C41 | 다건 시세 조회                | `GET /stocks/prices?stockCodes=005930,000660` **쉼표 구분 문자열.** 파라미터 이름은 **`stockCodes`로 확정**(`codes`·`tickers` 아님). **한 번에 최대 50건.** 요청한 코드 중 존재하지 않는 것은 `items`에서 제외되고 전체 요청이 실패하지 않는다                                                                                                                                                                                                                                                           | §5.5 · 이슈 #1 ②⑰       |
| C42 | `stale` 시세 스키마           | 세 상태다. **정상 수신**은 최신 값 + `stale: false`. **수신 끊김**은 `currentPrice`·`changeAmount`·`changeRate`가 **마지막 수신 값**, `asOf`가 마지막 수신 시각, `stale: true`. **값 없음(캐시 미스)**은 가격 3필드와 `asOf`가 **전부 `null`**, `stale: true`. 다건 조회는 **항목별로** 판정한다. 프론트는 `stale: true`면 "시세 지연"을 표시하고 값이 `null`이면 가격 영역을 비운다                                                                                                                     | §5.4 · §5.5 · 이슈 #1 ⑩ |

### 1.5 주문·계좌·종목

| #   | 항목                       | 확정된 값                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 출처                                        |
| --- | -------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- |
| C43 | 주문 유형                  | **시장가만.** 지정가·미체결 관리는 확장 범위다. 호가창 없음. 접수와 체결이 분리되지 않는다. **가격 입력 필드를 만들지 않는다**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | featureSpec §1.1 · §7.1                     |
| C44 | 거래 시간                  | 정규장 **09:00~15:30 KST** 외 주문 불가. 프론트는 주문 버튼 비활성화 + 사유 문구                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | featureSpec §1.1 · §7.4                     |
| C45 | 주문 비율 버튼             | **10% / 25% / 50% / 최대.** 분모는 `GET /orders/available`의 `maxQuantity`·`holdingQuantity`를 쓰고 화면이 계산하지 않는다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | featureSpec §7.3 · §7.3                     |
| C46 | 거래정지 종목              | `suspended: true`면 **뱃지 노출 + 매수·매도 차단.** 사유는 `suspendedReason`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | featureSpec §4 · §5.1 · §5.2                |
| C47 | 초기 예수금                | 최초 로그인 시 계정·가상 계좌·예수금 1,000,000원 자동 생성. 원장에 `INITIAL_GRANT` 한 줄로 남는다. **회차 개념은 v0.7 에서 없어졌다**(C26) — "1회차 생성"·"활성 회차 1개" 서술은 폐기됐다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | featureSpec §2.1~2.2(v2.2) · §2.1(v0.7)     |
| C49 | 충전 경로와 한도           | `GET /deposits/limit` · `POST /deposits`. 1회 10,000,000원, **계정 전체 누적** 100,000,000원. **v0.7 에서 기준이 회차 → 계정 전체로 바뀌었고 되돌릴 경로가 없다.** 응답 필드명도 함께 바뀌었다 — `roundCumulativeLimit`·`roundDepositedAmount` → **`cumulativeLimit`·`depositedAmount`**(`perRequestLimit`·`remainingAmount` 는 그대로). 에러 코드 접두사 **`DEPOSIT_*`**, 누적 초과는 `409 DEPOSIT_LIMIT_EXCEEDED` + `detail.remainingAmount`. **충전 취소 API는 없다.** 기준을 기간(월 등)으로 다시 정하면 이 항목과 응답이 또 바뀐다 — MR !78 본문 3번이 아직 답을 못 받은 전제라고 적었다                                                                                                                        | §4.1~4.2(v0.7) · §11 · 이슈 #1 ⑥ · 이슈 #27 |
| C50 | 관심 종목 최대             | 50개 (다건 시세 조회 최대치와 일치)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | featureSpec §6 · §5.5                       |
| C51 | 최근 본 종목               | 최대 30건 FIFO. **등록 API가 없다** — `GET /stocks/{stockCode}` 호출이 서버에 기록을 남긴다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | featureSpec §5 · §6.1                       |
| C81 | 최근 검색어                | **계정 기준 서버 저장이다** (ERD §2.11 `recent_search_keyword` — `user_id` 귀속). 최대 10건. `GET /stocks/search/recent` 응답은 `{items: [{keywordId(number), keyword(string), searchedAt(ISO 8601)}]}` 이고 **`searchedAt` 최신순**이다. **최근 본 종목(C51)과 달리 시세 필드가 없는 것이 의도다** — 검색어는 종목이 아니라 문자열이고 종목코드로 검색해도 문자열로 저장된다. **등록 API 가 없다** — `GET /stocks/search` 호출이 기록을 남기고, 같은 검색어 재검색은 새 행을 만들지 않고 `searchedAt` 만 갱신한다(`keywordId` 유지). `DELETE .../{keywordId}` · `DELETE .../recent` 는 **본문 없이 `204`** 이고 **없는 대상·남의 `keywordId` 도 `204`** 다 — 존재 여부 자체가 정보 노출이라 구분하지 않는다(구 P19) | §6.2(v0.6) · §11.2 · 이슈 #23 회신          |
| C76 | 전량 매도 종목의 `holding` | `GET /stocks/{stockCode}`의 `holding`은 **전량 매도 시 `null`로 내려온다.** `quantity = 0` 행은 재매수 시 INSERT 경합을 피하기 위한 DB 내부 구현일 뿐 API로 노출되지 않는다(`GET /portfolio`의 `quantity > 0` 필터와 동일 적용). **프론트는 `holding !== null`로 보유 블록 렌더 여부를 판단한다**(구 P17)                                                                                                                                                                                                                                                                                                                                                                                                            | §5.2(v0.5) · 이슈 #19 ①                     |
| C77 | 검색 결과의 상장폐지 종목  | 검색 쿼리에 `is_active = true` 조건이 걸려 **상장폐지 종목은 검색 결과에서 제외된다.** 구분 필드는 응답에 없다(구 P18)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | §5.1(v0.5) · 이슈 #19 ②-1                   |
| C78 | 보유 중 상장폐지 종목      | **MVP 범위에서 발생하지 않는다.** 종목 마스터는 프로젝트 기간 중 고정 적재이고 기간 중 상장폐지를 반영하는 배치가 없다. `is_active`는 ERD에는 있지만 검색·상세·보유 목록 응답에 구분 필드가 추가되지 않는다. **프론트는 이 분기를 만들지 않는다**(구 P18)                                                                                                                                                                                                                                                                                                                                                                                                                                                            | §5.1(v0.5) · 이슈 #19 ②-2                   |

### 1.6 AI 응답 구조

| #   | 항목                                         | 확정된 값                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 출처                                      |
| --- | -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- |
| C52 | AI 인도 상태                                 | **Phase 1·2·3 전부 `master`에 머지됐다.** 프론트가 AI 파트의 인도를 기다릴 이유가 없다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | AI 명세 §13 · 이슈 #8 회신                |
| C53 | AI 응답 조립 책임                            | 프론트는 완성된 응답을 그대로 출력한다. 수치 치환·검증은 AI 서버가 끝낸다. **예외 둘** — `wiki`의 `ai_inferred` 항목은 화면에서 단정형으로 렌더하지 않고("~하신 것으로 보이는데 맞나요?" 형태), `chat`의 `answer.title`이 항상 `null`이라 말풍선 제목은 프론트가 정한다                                                                                                                                                                                                                                                                                                                                                                     | AI 명세 §1 · §2.3 · §9                    |
| C54 | AI 응답의 `null` 규약                        | **선언된 키는 값이 없어도 빠지지 않고 `null`로 온다.** 프론트는 **키 존재 여부가 아니라 `null` 여부로** 분기하고, Zod는 `.optional()`이 아니라 **`.nullable()`**로 짠다. 유일한 예외는 `analysis`의 `AnalysisSection` 내부 필드로 이쪽만 `.optional()` 대상이다(P8)                                                                                                                                                                                                                                                                                                                                                                         | AI 명세 §2.2 · MR !23 검토 §3.2           |
| C55 | `text`와 `segments`                          | `segments`를 이어 붙이면 `text`와 정확히 일치한다. **`text`만 출력해도 정상 동작하는 기본 렌더링 경로이고**, 수치 스타일링이 필요할 때만 `segments`를 순회한다. Segment 6키(`type` `value` `raw` `unit` `source` `direction`)는 `text` 조각에도 전부 실려 나오므로 판별 유니언으로 짜지 않는다. `direction`은 `up`/`down`/`null`이고 국내 관례에 따라 적색·청색이다                                                                                                                                                                                                                                                                         | AI 명세 §2.3 · §12                        |
| C56 | 구현 현황이 만드는 제약                      | `cached`는 항상 `false`, `cached_at`은 항상 `null`(캐시 계층이 없다)이라 **캐시 배지 UI를 만들 이유가 없다.** `briefing.status`에 `generating`이 오지 않아 **스켈레톤 + 재조회 경로가 필요 없다.** `thesis_check.supporting`·`challenging`·`next_events.events`·`briefing.items[].citations`는 **항상 빈 배열**이므로 빈 배열을 실패로 다루지 않는다. `attribution`의 `benchmark`는 받기만 하고 쓰이지 않아 **벤치마크 선택 UI를 만들지 않는다.** `attribution`의 중복 키 3쌍(`portfolioReturn`/`totalReturn`, `summary`/`text`+`segments`, `events[].title`/`summary`)은 **셋 다 남기는 것으로 정리됐고 프론트는 각 쌍에서 한쪽만 읽는다** | AI 명세 §2.2 · §6 · §8 · 이슈 #14 철회    |
| C57 | `analysis`의 `sections` 키 집합              | 요청·응답 모두 7개 키(`current`·`changes`·`attention`·`risks`·`my_impact`·`thesis_check`·`next_events`)로 **계약 고정.** 목록 밖 키를 요청하면 서버가 400으로 거부한다                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 이슈 #16 회신(2026-08-28)                 |
| C58 | `personalize=false`일 때 개인화 섹션         | `my_impact`·`thesis_check` **키는 남고 값이 `null`이다.** 서버가 7칸을 먼저 만들고 개인화 섹션의 생성만 건너뛴다. 키 존재 여부가 아니라 값으로 분기한다(C54 규약과 동일 패턴)                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 이슈 #16 회신(2026-08-28)                 |
| C59 | `AnalysisSection`의 실제 필드                | `title`·`text`·`segments`·`cached`·`cached_at`·`thesis`·`supporting`·`challenging`·`events` 9개가 실제 응답에 실린다. `openapi.json`이 빈 object로 보이는 것은 `OptionalKeysModel`의 wrap 직렬화기가 만드는 **스키마 표시 버그**이고 실제 응답 값과는 무관하다고 AI 파트가 원인을 확인했다. **이 스키마 수정 자체는 아직 master에 반영되지 않았다** — P8 참고                                                                                                                                                                                                                                                                               | 이슈 #15 회신(2026-08-28)                 |
| C60 | `PUT /wiki/theses/{ticker}` 요청 본문        | `POST /wiki/theses`와 같은 `ThesisIn`(`ticker`·`text` 필수, `horizon`·`linked_trade_id` 선택). 서버는 **경로의 `ticker`를 기준으로 처리하고 본문의 `ticker` 값은 무시**한다. 다만 호환을 위해 프론트는 본문에도 `ticker`를 채워 보낸다                                                                                                                                                                                                                                                                                                                                                                                                      | 이슈 #13 회신(2026-08-28)                 |
| C61 | `PUT`·`DELETE /wiki/*` 응답 본문             | 둘 다 **공통 응답 봉투.** `PUT`의 `content`는 갱신된 논지 전체, `DELETE`의 `content`는 `{id, deletedAt}`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 이슈 #13 회신(2026-08-28)                 |
| C62 | `POST /ai/feedback` 응답 본문                | **공통 응답 봉투**, `content`는 `{recorded: true}`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 이슈 #13 회신(2026-08-28)                 |
| C63 | `INSUFFICIENT_DATA`의 `detail.reason`        | 값은 `llm_key_missing`·`ledger_unavailable` **두 개가 전부다.** 단 **모든 `INSUFFICIENT_DATA`가 `reason`을 갖지는 않는다** — "보유 종목 없음"·"거래일 없음"·"종가 없음" 같은 경우는 `message`만 있고 `detail`이 빈 객체로 온다. 프론트는 `detail.reason`이 없을 수 있다는 전제로 분기해야 한다                                                                                                                                                                                                                                                                                                                                              | 이슈 #13 회신(2026-08-28)                 |
| C79 | `wiki`·`feedback` 전용 content pydantic 모델 | C60\~C63의 값은 원래 `Envelope[dict]`(자유 형식 `content`)를 전제로 확인된 것이었다. **11:02 회신에서 AI 파트가 기능별 전용 모델(`WikiContent`·`WikiFactOut`·`WikiThesisOut`·`DeletedFactContent`·`FeedbackContent`)을 추가하고, 이 다섯 경로가 더 이상 `Envelope[dict]`로 생성되지 않는 OpenAPI 계약 테스트도 넣었다고 확인했다.** `ai/docs/openapi.json`에 다섯 타입명이 실제로 존재하는 것으로 프론트가 직접 확인했다. **이제 이 다섯 경로도 `openapi.json`에서 키 목록·nullable 여부를 그대로 읽을 수 있다** — C57\~C63의 "손으로 옮겨 적어야 한다"는 제약(구 P5)이 이 다섯 경로에 한해 해소됐다                                        | 이슈 #13 11:02 회신                       |
| C64 | 논지(`theses`) 삭제 수단                     | **없다.** `status`를 바꾸는 `PUT`도 삭제가 아니다. 새 논지가 기록되면 기존 `active` 논지가 서버 내부에서 자동으로 `closed`가 될 뿐, 사용자가 부를 수 있는 삭제 API가 없다. **화면은 논지 행에 삭제 컨트롤을 두지 않는다**                                                                                                                                                                                                                                                                                                                                                                                                                   | 이슈 #17 회신(2026-08-28)                 |
| C65 | 논지 수정의 반영 시점                        | `PUT`으로 고친 논지는 저장 직후부터 다음 AI 응답에 반영된다. AI 명세의 "대화 종료 후 비동기 배치"는 AI가 대화에서 새 사실을 추출하는 별도 과정이고 **사용자의 직접 수정에는 적용되지 않는다.** "반영까지 시간이 걸립니다" 안내는 넣지 않는다                                                                                                                                                                                                                                                                                                                                                                                                | 이슈 #17 회신(2026-08-28)                 |
| C66 | 피드백 중복 전송                             | 같은 `requestId`로 다시 보내면 **마지막 값으로 덮어쓴다.** 평가 행이 누적되지 않는다. **취소 API는 없다.** 전송 성공 시 슬롯을 잠그고, 재전송(수정)은 열어 둘 수 있되 완전한 취소 버튼은 만들지 않는다                                                                                                                                                                                                                                                                                                                                                                                                                                      | 이슈 #17 회신(2026-08-28)                 |
| C67 | AI `UNAUTHORIZED`(401)와 사용자 세션         | 백엔드-AI 서버 사이의 **내부 인증 문제이며 사용자 로그인 만료가 아니다.** 이 코드를 받아도 **재로그인시키지 않는다.** `frontConvention.md` §5의 화이트리스트 방식(코드 문자열로만 판단, C23)이 이미 이 전제를 반영하고 있다. AI 명세 문서의 "프론트 처리: 재로그인" 표기는 AI 파트가 수정 대상으로 확인했다                                                                                                                                                                                                                                                                                                                                 | 이슈 #17 회신 · 이슈 #18 회신(2026-08-28) |
| C68 | 에러 `code`의 `AUTH_` 이름공간               | `AUTH_` 접두는 **우리 서비스 사용자 인증 전용이다.** AI 서버는 앞으로 `AUTH_` 접두 코드를 발행하지 않는다. C23·C67의 화이트리스트 판단이 성립하는 전제가 이것이다. **단 이 규칙이 아직 문서에 한 줄로 적히지 않았다** — AI 파트는 `ai/docs/api-spec.md`에 적겠다고 했고 백엔드 파트에도 같은 줄을 요청했지만, 이슈 #18의 백엔드 담당(`ehdgurdusdn`) 회신은 아직 없다                                                                                                                                                                                                                                                                        | 이슈 #18 회신(2026-08-28, AI 파트만)      |

| C80 | AI 위키 3종 중계 경로 확정 | `GET /api/v1/ai/wiki` → `GET /api/ai/v1/wiki`, `PUT /api/v1/ai/wiki/theses/{stockCode}` → `PUT /api/ai/v1/wiki/theses/{ticker}`, `DELETE /api/v1/ai/wiki/facts/{factId}` → `DELETE /api/ai/v1/wiki/facts/{factId}`. 세 경로가 §10.1 표에 정식으로 실렸다(구 P3). **`POST /api/ai/v1/wiki/theses`는 AI 서비스가 내부에서 스스로 호출하는 경로라 중계 대상이 아니다 — 프론트 호출 경로가 없다.** | §10.1(v0.5) · 이슈 #12 · 이슈 #7 회신 |

확정 **81건**. (v0.7 에서 C48 계좌 리셋이 폐기돼 82 → 81 이 됐다. C26·C47·C49 는 값이 갱신됐다.)

**C57~C59는 값 자체가 확정된 것이지, `openapi.json`에서 타입을 자동 생성할 수 있게 됐다는 뜻이 아니다.** AI 파트가 원인·수정 방식·계약 값을 답했을 뿐 `AnalysisSection`의 실제 스키마 수정과 계약 테스트 추가는 아직 master에 없다(P8 참고). **C60~C63(wiki·feedback)은 C79로 이 제약이 풀렸다** — 전용 pydantic 모델과 계약 테스트가 이미 master에 반영돼 있다. 프론트는 `analysis` 하나만 지금도 손으로 Zod 스키마를 옮겨 적어야 한다.

---

## 2. 잠정 확정 — 프론트가 임의로 정한 것

**전부 잠정이다.** 회신이 오면 그 값이 우선한다.
근거가 얇은 것도 있지만 **고칠 범위가 작은 쪽을 골랐다.** 되돌리는 비용이 보이지 않으면 잠정으로 정하지 않는다.

| #   | 항목                        | 정한 값                                                                                                    | 근거                                                                                                                                                                          | 틀렸을 때 고칠 범위                                                                            |
| --- | --------------------------- | ---------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| T1  | dev proxy 규칙              | `server: { proxy: { '/api': 'http://localhost:8080' } }` 하나                                              | 프론트가 AI 서버를 직접 부르지 않으므로(C1) 프록시 규칙을 나눌 필요가 없다                                                                                                    | `vite.config.ts` 한 줄                                                                         |
| T2  | AI 응답 키의 camelCase 결과 | 숫자가 섞인 이름은 `top1Weight` · `maxDrawdown1y`로 가정한다                                               | §10.3이 camelCase 재포장을 정했지만 `top1_weight`·`max_drawdown_1y` 같은 이름의 변환 결과가 규칙만으로 정해지지 않고 라이브러리마다 다르다. `return`은 예약어라 더 불확실하다 | AI 응답 Zod 스키마의 키 이름. **AI 계열 스키마를 한 디렉토리에 모아 두면 그곳만 고친다**       |
| T3  | `openapi.json`의 사용 범위  | **키 목록과 nullable 여부의 사실 원천으로만 쓰고** 런타임 검증 Zod 스키마는 camelCase로 프론트가 직접 쓴다 | 생성 타입은 AI 서버 스키마(snake_case)이고 프론트가 받는 것은 백엔드가 재포장한 응답이다(C7). 생성 타입을 그대로 런타임 검증에 쓸 수 없다                                     | 생성 타입을 직접 쓰는 쪽으로 바꾸면 API 레이어의 검증 계층 전체. **화면 컴포넌트는 안 바뀐다** |

잠정 확정 **3건**.

**T2가 되돌릴 비용이 가장 크다.** AI 응답 스키마가 feature마다 흩어지면 회신이 왔을 때 고칠 곳을 찾는 것부터 일이 된다.

---

## 3. 미확정

미확정 항목은 **GitLab 이슈로 추적한다.** 이슈가 없는 것은 그 사유를 적었다.

| #   | 항목                                                              | 무엇을 기다리나                                                                                                                                                                                                                                                                                  | 누구 답을 기다리나 | 추적                                                                                                                                                                                                                                                 | 프론트에 미치는 영향                                                                                                                                                                                                                                                                                                 |
| --- | ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P7  | AI 6종 `content` 키 구성                                          | MR !23(`S15P21A101-84`)이 6종 `content`를 pydantic 타입으로 정의하고 `openapi.json`을 재생성했다. **머지됐다**(커밋 `22d1233`)                                                                                                                                                                   | AI 파트            | 머지 완료. 후속 질문은 이슈 [#15](https://lab.ssafy.com/s15-fintech-finance-sub1/S15P21A101/-/issues/15)·[#16](https://lab.ssafy.com/s15-fintech-finance-sub1/S15P21A101/-/issues/16)로 이관, **둘 다 닫힘, 회신 있음(2026-08-28)** — 내용은 P8 참고 | `chat`·`diagnosis`·`attribution`·`preview`·`briefing` 5종은 키 구성과 nullable 여부가 스키마로 확정됐다. `analysis`만 남은 문제는 P8로 이어진다                                                                                                                                                                      |
| P8  | `AnalysisSection`의 `openapi.json` 반영                           | 원인(`OptionalKeysModel`의 wrap 직렬화기)·수정 방식(직렬화기 제거 + `response_model_exclude_unset=True`)·`sections` 7키 계약·`personalize=false` 시 null 처리는 모두 확인됐다(→ C57~C59). **남은 것은 그 수정 자체를 master에 반영하는 것과 계약 테스트 추가뿐이다**                             | AI 파트            | 이슈 [#15](https://lab.ssafy.com/s15-fintech-finance-sub1/S15P21A101/-/issues/15) · 이슈 [#16](https://lab.ssafy.com/s15-fintech-finance-sub1/S15P21A101/-/issues/16), **둘 다 회신 있음(2026-08-28), 수정 반영 대기**                               | 값 자체는 확정됐으므로(C57~C59) 프론트는 손으로 적은 Zod 스키마로 지금 바로 진행할 수 있다. `openapi.json`에서 자동 타입 생성이 가능해지는 시점만 남았다. `AiSlot` 껍데기로 우회해 왔던 종목 상세 화면도 이제 손으로 쓴 스키마로 실제 렌더로 바꿀 수 있다                                                            |
| P9  | 웹소켓 전환 시점                                                  | `[S0-2]` 결정                                                                                                                                                                                                                                                                                    | 8/26 결정 회의     | 이슈 없음 (스프린트 0 결정)                                                                                                                                                                                                                          | 폴링 선행 구현으로 진행한다(C34). 폴링 주기 자체는 프론트 재량으로 확정됐으므로(C40) 상수 값을 기다릴 필요가 없다                                                                                                                                                                                                    |
| P10 | `stale` 허용 시간과 주문 차단 기준                                | `[S0-3]` 결정                                                                                                                                                                                                                                                                                    | 8/26 결정 회의     | 이슈 없음 (스프린트 0 결정)                                                                                                                                                                                                                          | "시세 지연" 표시 임계값과 주문 버튼 비활성화 조건을 정할 수 없다. 관계식상 KIS 순회 주기(3초)보다 커야 하고 최소 10초 이상이 권장된다                                                                                                                                                                                |
| P11 | 분봉 데이터 제공 여부                                             | `[S0-4]` 결정                                                                                                                                                                                                                                                                                    | 8/26 결정 회의     | 이슈 없음 (스프린트 0 결정)                                                                                                                                                                                                                          | 차트 기간 탭 구성이 달라진다. 일봉(`1M`·`3M`·`1Y`) 기준으로 먼저 만들고 탭은 데이터 주도로 구성한다                                                                                                                                                                                                                  |
| P22 | `GET /transactions?type=DEPOSIT` 이 `INITIAL_GRANT` 를 포함하는가 | apiSpec §8.2 는 `type` 필터 값 4종(`ALL`·`BUY`·`SELL`·`DEPOSIT`)과 원장 유형 4종(`INITIAL_GRANT`·`DEPOSIT`·`BUY`·`SELL`)을 각각 적었지만 **둘의 대응을 적지 않았다.** 필터에 `INITIAL_GRANT` 값이 없으므로 초기 지급 행이 `DEPOSIT` 에 섞여 오는지, 아니면 `ALL` 로만 보이는지가 정해지지 않았다 | 백엔드             | **MR [!78](https://lab.ssafy.com/s15-fintech-finance-sub1/S15P21A101/-/merge_requests/78) 에 코멘트로 질의**(2026-09-03, 회신 대기)                                                                                                                  | 매매 내역 화면의 "입금" 탭 내용이 갈린다. **목은 포함하는 쪽으로 만들어 뒀다** — 사용자 입장에서 초기 지급 1,000,000원도 입금으로 읽히고, 빼면 `type=DEPOSIT` 합계가 예수금 유입과 어긋난다. 가정은 `mocks/handlers/trading.ts` 의 `TRANSACTION_FILTER_LEDGER_TYPES` 표 한 줄에 갇혀 있어 회신이 오면 그 줄만 고친다 |

미확정 **6건**.

### 이번 판에서 해소된 것

- **KIS 실시간 등록 한도 수치** — 해소됐다. **프론트 관심사가 아니다.** §5.6이 한도를 프론트 계약에
  노출하지 않기로 못박았고 백엔드 수집 계층이 흡수한다(C35). `[S0-1]` 실측값이 얼마로 나와도
  프론트 계약은 변하지 않는다. 한도 수치를 코드 상수로 박지 않는다는 방침은 그대로 유효하되,
  이유가 "값이 아직 미정이라서"에서 "프론트가 알 필요가 없어서"로 바뀌었다.
- **구독·해제 신호의 전달 채널** — §5.6으로 확정돼 C37로 옮겼다.
- **시세 갱신 주기** — 폴링 주기가 프론트 재량, 서버 보장이 TTL 30초로 확정됐다(C40).
  남은 것은 웹소켓 전환 시점뿐이라 P9로 좁혔다.
- **`GET /stocks/prices`의 stale 항목 스키마** — §5.4의 3상태 표로 확정돼 C42로 옮겼다.
- **AI 장애와 백엔드 장애를 구분할 에러 코드** — §10.4로 확정돼 C11로 옮겼다.
- **AI 중계 엔드포인트 경로 문자열** — §10.1의 7종으로 확정돼 C3으로 옮겼다.
- **AI Phase 1 인도 시점** — 인도 완료로 확정돼 C52로 옮겼다.
- **논지 삭제 수단(구 P12)** — 이슈 #17 회신으로 "삭제 API 없음"이 확정돼 C64로 옮겼다.
- **사용자 위키 편집의 반영 시점(구 P13)** — 이슈 #17 회신으로 "저장 직후 반영"이 확정돼 C65로 옮겼다.
- **엔드포인트별 에러 `code` 전체 목록(구 P6)** — apiSpec v0.4 §11.1·§11.2로 확정돼 C69로 옮겼다.
  GitLab 이슈 #4 자체는 완료 판정(OpenAPI 산출물 반영)이 아직이라 열려 있다.
- **에러 응답의 `requestId` 유무(구 P16)** — apiSpec v0.4 §10.4로 확정돼 C70으로 옮겼다.
  `AI_UPSTREAM_*` 둘은 `requestId`가 없고, 나머지 AI 서버 발행 코드는 보존된다.
- **계좌 리셋·회차 관련 계약의 개정 시점** — apiSpec v0.7(MR !78)로 회신이 와서 해소됐다.
  회차와 리셋이 **제거**되는 방향이었다. C26을 계좌 규칙으로 다시 쓰고, C47에서 회차 서술을 빼고,
  C49의 한도 기준을 계정 전체로 바꿨으며 **C48(계좌 리셋)은 폐기했다.**
  대신 `type=DEPOSIT` 의 `INITIAL_GRANT` 포함 여부가 새로 열려 P22가 됐다.
- **충전 누적 한도의 기준(이슈 #27 "함께 정해야 하는 것" 1번)** — 계정 전체 누적으로 확정돼 C49에 반영했다.
  다만 MR !78 본문 3번은 이것을 "답을 못 받은 상태에서 손해가 없는 쪽으로 고른 전제"라고 적었다.
  기간(월 등) 기준으로 다시 정해지면 C49와 `GET /deposits/limit` 응답이 또 바뀐다.
- **피드백 중복 전송 정책(구 P14)** — 이슈 #17 회신으로 "덮어쓰기, 취소 API 없음"이 확정돼 C66으로 옮겼다.
- **웹소켓 연결 수명 관리(구 P1)** — 이슈 #10 회신으로 CONNECT 인증 실패 응답·연결 유지 중 토큰 만료 처리·SockJS 폴백 여부가 모두 확정돼 C71~C73으로 옮겼다.
- **AI 응답 본문의 종목코드 필드 이름(구 P2)** — 이슈 #11 회신으로 "`ticker` 계열 그대로, 매핑 표 없음"이 확정돼 C74로 옮겼다.
- **재포장 후 남는 봉투 필드(구 P4)** — 이슈 #10 ⑤번 회신으로 "§10.3 표의 넷뿐, `generatedAt`·`model`·`cached`는 걷어냄"이 확정돼 C7로 옮겼다.
- **`wiki`·`feedback` 전용 content pydantic 모델(구 P5)** — 이슈 #13 11:02 회신으로 전용 모델과 계약 테스트 반영이 확인돼(직접 `openapi.json` 대조 완료) C79로 옮겼다.
- **AI 중계 요청 본문의 키 표기(구 P15)** — 이슈 #12 ③번 회신으로 "요청도 camelCase, 양방향 대칭"이 확정돼 C75로 옮겼다.
- **전량 매도한 종목의 `holding` 처리(구 P17)** — 이슈 #19 ①번 회신으로 "`holding: null`"이 확정돼 C76으로 옮겼다.
- **상장폐지 종목 노출(구 P18)** — 이슈 #19 ②번 회신으로 "검색 결과 제외 + 보유 중 상장폐지는 MVP 범위 밖"이 확정돼 C77·C78로 옮겼다.
- **AI 위키 3종의 중계 경로(구 P3)** — apiSpec v0.5 §10.1 표에 위키 3종 경로가 실제로 실리고
  `POST /wiki/theses` 중계 제외가 명문화돼 C80으로 옮겼다. 값 자체는 이슈 #7 회신으로 이미 알려져
  있었고, 남아 있던 것은 문서 반영뿐이었다.
- **최근 검색어 응답 본문과 저장 위치(구 P19)** — 이슈 #23 회신(2026-09-02)에 이어 **apiSpec v0.6 §6.2 에
  문서로 명문화까지 끝나** C81로 옮겼다. 회신 내용과 문서 내용이 세 항목 모두 일치해 어긋난 자리가 없다
  (계정 기준 서버 저장 · `keywordId`·`keyword`·`searchedAt` 3필드 `items` 배열 최신순 · `DELETE` 는
  본문 없이 `204`, 없는 대상·타인 `keywordId` 도 `204`). **발행부터 확정까지 하루가 걸리지 않았다.**

---

## 4. 문서 간 충돌

**공통 API 규격 v0.3이 기준에서 폐기되면서 이전 판의 충돌 9건 중 8건이 소멸했다.**
값을 대조할 한쪽이 없어졌기 때문이지, 값이 합의된 것이 아니다. 다만 대조 결과 v0.3에만 있던
값(`isSuccess` 봉투, `CHARGE_*` 접두사, `?tickers=`, 비율 0~1 소수 일괄 적용)은 모두 채택되지 않았고
`apiSpec.md` v0.2의 값이 확정으로 올라갔다. 남은 충돌은 아래 하나다.

| #   | 항목                   | 어느 문서가 뭐라고 하나                                                                                                                                                                                                         | 따르기로 한 것                                                                                                                                                                                                                                                                                                                                                                 |
| --- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| X1  | AI 명세 파일이 두 개다 | [`docs/api/aiApiSpec.md`](../../docs/api/aiApiSpec.md) — 8/20 시점 사본(커밋 `5693aea`). [`ai/docs/api-spec.md`](../../ai/docs/api-spec.md) — AI 파트 원본이고 8/21에 갱신됐다(커밋 `ff1c29c`, MR !21). 두 파일의 내용이 다르다 | **`ai/docs/api-spec.md`를 따른다.** 갱신이 이쪽에만 반영됐다 — 봉투의 `macro` 키, `thesis_conflicts` 이름 변경, Segment 6키 서술이 글로벌 사본에는 없다. **문제는 `apiSpec.md` §10.4와 §11이 AI 에러 코드 전체 목록의 소유를 글로벌 사본으로 지목한다는 것이다.** 낡은 쪽을 가리키고 있다. 두 파일 중 하나는 정리돼야 하지만 둘 다 프론트 소유가 아니므로 고치지 않고 보고한다 |

충돌 **1건**.

### 그 외 관측한 것

- **`featureSpec` §10.2가 6종으로 정정됐다.** 이전 판이 "낡은 서술"로 다뤘던 충돌이 커밋 `ba3bf23`으로
  해소됐고, 프론트가 부르는 중계 경로까지 `/api/v1/ai/...`로 정확히 적혔다. GitLab 이슈 #9의 완료 판정이
  충족돼 **이슈도 닫혔다.**
- **AI 서버에는 6종 밖에 `POST /feedback`과 위키 4종이 구현돼 있다.** 2026-08-24에
  `ia.md`가 "AI가 이해한 나" 화면과 피드백 배치 규칙을 등재해 **화면 범위가 정해졌다.**
  피드백은 `requestId` 하나가 슬롯 하나이고, 에러 봉투에도 `requestId`가 있으므로 실패 응답과
  `INSUFFICIENT_DATA` 대체 문구 자리에도 붙는다. 위키는 화면에서 `GET`·`PUT`·`DELETE` 셋만
  부르고 `POST /wiki/theses`는 AI 서비스가 스스로 호출한다. **이것이 구 P3(위키 중계 경로, → C80) 확정의
  선행 조건이었다** — `apiSpec.md` §10.1이 그 경로 3종을 v0.5에서 정식으로 표에 실었다.
- **`429 RATE_LIMITED`는 AI 명세에 정의만 있고 세는 곳이 없어 현재 나가지 않는다.** 재시도 대기 로직을
  만들되 실제로 걸리는 것을 전제하지 않는다.
- **`ai/docs/api-spec.md`와 `ai/docs/openapi.json`이 벌어져 있다.** 어긋나면 구현(`openapi.json`)이 맞다.
- **AI 파트가 다른 파트에 요구하는 것은 둘뿐이다** — 서버 간 인증 수단과, 화면에서 AI 응답이 들어갈
  빈 슬롯이다. AI 명세 §11이 그렇게 적고 있다. 프론트가 만들 것은 슬롯이고, 그 안에 들어가는
  응답 내용의 책임은 AI 파트에 있다(C53).
- **`ia.md`(MR !50, 2026-08-28 개정)의 §7 미확인 항목 표가 이 문서보다 낡았다.** 같은 날 두 문서가
  각자 개정됐는데 `ia.md` 쪽이 반영을 놓쳤다 — "시세 갱신 주기"(C40으로 해소), "AI 장애와 백엔드
  장애를 구분할 코드"(C11로 해소), "다건 시세 조회 파라미터 이름"(C41로 해소, `stockCodes` 확정),
  "`wiki`·`feedback` 응답 본문"(C60~C63으로 해소), "논지 삭제 수단의 부재"(C64로 해소), "사용자 위키
  편집의 반영 시점"(C65로 해소), "피드백 중복 전송 정책"(C66으로 해소) 일곱 줄이 여전히 미확정으로
  남아 있다. `ia.md`는 다른 사람 소유 문서라 여기서 고치지 않고 보고만 한다.

---

## 5. 발송 대기 중인 질문

`_inbox/2026-08-24-질문-위키-피드백-미발송.md`에 있던 AI 파트 3건(구 P12·P13·P14)은 이슈
[#17](https://lab.ssafy.com/s15-fintech-finance-sub1/S15P21A101/-/issues/17)로 이미 발송돼
2026-08-28에 회신까지 왔다(→ C64~C66). 같은 파일에 있던 백엔드 대상 2건(구 P15·P16)은 이슈
[#12](https://lab.ssafy.com/s15-fintech-finance-sub1/S15P21A101/-/issues/12) ③·④번으로 물었고
2026-09-02에 둘 다 회신이 와 각각 C75·C70으로 옮겼다. **이 워크트리에는 `_inbox/`가 없어 그 파일
자체가 아직 `done/`으로 옮겨졌는지는 확인하지 못했다(불확실).**

이전 판에 남아 있던 두 파일(`_inbox/2026-08-20-질문-백엔드-2차-미발송.md` ·
`_inbox/2026-08-20-질문-AI파트-2차-미발송.md`)은 GitLab 이슈로 올라가 `_inbox/done/`으로 옮겨졌다.

미확정 항목의 추적 경로는 §3 표의 "추적" 열이다. 이슈가 없는 것은 셋이다 —
스프린트 0 결정 3건(**P9·P10·P11**).
**P3은 apiSpec v0.5 §10.1로 문서 반영까지 끝나 확정으로 옮겨갔다(→ C80).**
**P7은 MR !23 머지로 해소됐다(C52 근방). P8은 이슈 [#15](https://lab.ssafy.com/s15-fintech-finance-sub1/S15P21A101/-/issues/15)·
[#16](https://lab.ssafy.com/s15-fintech-finance-sub1/S15P21A101/-/issues/16) 회신으로 계약 값은 확정됐지만(C57\~C59)
`AnalysisSection`의 `openapi.json` 반영은 아직 남아 열려 있다.**
**P3·P5·P15·P17·P18은 이전 판에서 전부 확정으로 옮겨갔다(→ C80·C79·C75·C76·C77·C78).
이슈 [#23](https://lab.ssafy.com/s15-fintech-finance-sub1/S15P21A101/-/issues/23)(최근 검색어 응답 본문)으로
등재됐던 P19 도 회신과 apiSpec v0.6 §6.2 명문화로 확정됐다(→ C81). 이슈는 닫혔다.**

**남은 미확정 6건 가운데 팀 회신을 기다리는 것은 P8·P22 둘이다.** 나머지 넷(P7·P9·P10·P11)은
회신이 아니라 우리 쪽 반영이나 스프린트 결정을 기다리는 항목이다.
**P22 는 apiSpec v0.7 을 반영하다 새로 생긴 항목이다** — 회차가 없어지면서 원장 유형 4종과
`type` 필터 4종이 나란히 놓였는데 둘의 대응이 명세에 없다. MR !78 에 물어 뒀다.
