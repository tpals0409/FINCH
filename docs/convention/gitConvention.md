```java
———————[ 개발 ]———————
저희 개발 규칙은 이슈 생성 -> MR -> 코드리뷰 -> 머지 순서로 가겠습니다.

이슈는 기본적으로 자신이 어떤 기능을 개발/버그 수정 등등을 할 것인지 
미리 명시하는 작업입니다.

———————[ 개발 시나리오 ]———————
1. Issue를 만든다.
작은 단위로 개발할 내용을 기반하여 Issue를 만듭니다.

2. 브랜치를 만든다.
생성된 Issue 번호를 기준으로 브랜치 이름을 만들어서 생성합니다.
브랜치 네이밍 규칙은 스페이스번호-티켓번호-{기능명} 입니다.
브랜치 이름 -> S1241AS1-23-auth

3. 브랜치 위에서 개발 하고 브랜치에 푸시한다.

4. 해당 이슈에 대한 개발이 완료되면 MR을 생성한다.

5. MM에 MR 리뷰해 달라고 요청하고, 팀원들은 코드를 리뷰한다.

6. 리뷰가 완료되면 master 브랜치에 머지한다.

7. 해당 Issue를 닫고, 브랜치를 삭제한다.
————————————————

앞으로 열심히 달려봅시다 🏃‍♀️ 
긴가민가하다 -> 질문
잘 모르겠다 -> 질문
이렇게 하는거 맞나? -> 질문
이런거 물어보면 귀찮겠지? -> 질문
```

```
예시)
내가 회원가입 기능을 개발했다
feat: 회원가입 기능 개발

내가 회원가입 로직을 수정했다
refactor: 회원가입 로직 수정

내가 회원가입 로직 버그를 수정했다
fix: 회원가입 로직 버그 수정
```

> **이모지를 붙이지 않습니다.** (2026-08-20 팀 회의 확정)
> `<타입>: <제목>` 형식만 사용합니다. 콜론 앞에 공백을 넣지 않습니다.
>
> - O: `feat: 회원가입 기능 개발`
> - X: `✨feat: 회원가입 기능 개발`
> - X: `feat : 회원가입 기능 개발`

## 🖤 커밋 메시지 컨벤션

<aside>
✅

### 1. 커밋 유형 지정

- 커밋 유형은 영어 소문자로 작성하기 (2026-07-29 실제 커밋 관행에 맞춰 대문자 → 소문자로 수정)
- **이모지는 사용하지 않기** (2026-08-20 팀 회의 확정)
- 형식은 `<타입>: <제목>` — 콜론 뒤에만 공백 한 칸
    
    
    | 커밋 유형 | 의미 |
    | --- | --- |
    | `feat` | 새로운 기능 추가 |
    | `fix` | 버그 수정 |
    | `docs` | 문서 수정 |
    | `style` | 코드 formatting, 세미콜론 누락, 코드 자체의 변경이 없는 경우 |
    | `refactor` | 코드 리팩토링 |
    | `test` | 테스트 코드, 리팩토링 테스트 코드 추가 |
    | `chore` | 패키지 매니저 수정, 그 외 기타 수정 ex) .gitignore |
    | `design` | CSS 등 사용자 UI 디자인 변경 |
    | `comment` | 필요한 주석 추가 및 변경 |
    | `rename` | 파일 또는 폴더 명을 수정하거나 옮기는 작업만인 경우 |
    | `remove` | 파일을 삭제하는 작업만 수행한 경우 |
    | `!BREAKING CHANGE` | 커다란 API 변경의 경우 |
    | `!HOTFIX` | 급하게 치명적인 버그를 고쳐야 하는 경우 |

### 2. 본문은 필요할 때만 (기본은 제목 한 줄)

- **제목만으로 충분하면 본문을 쓰지 않는다.** 대부분의 커밋은 한 줄로 끝난다.
- 본문이 필요한 경우: 왜 그렇게 했는지가 제목만으로 안 드러날 때, 주의할 부작용이 있을 때
- 본문을 쓸 때는 제목과 빈 행으로 분리하고, **무엇을 했는지가 아니라 왜 했는지**를 적는다
- 제목과 본문은 한글로 작성하여 내용이 잘 전달될 수 있도록 할 것

```
# 좋음 — 제목만으로 충분
docs: 기능 명세서 v2.1 추가

# 나쁨 — 본문이 제목을 반복할 뿐
feat: 회원가입 기능 추가

- 회원가입 기능 추가
```

### 3. 제목 끝에는 `.` 금지

### 4. 제목은 영문 기준 50자 이내로 할 것

### 5. 자신의 코드가 직관적으로 바로 파악할 수 있다고 생각하지 말자

### 6. 여러가지 항목이 있다면 글머리 기호를 통해 가독성 높이기

```
- 변경 내용 1
- 변경 내용 2
- 변경 내용 3
```

</aside>

### 🖤 규칙에 맞는 좋은 커밋메시지를 작성해야 하는 이유

- 팀원과의 소통
- 편리하게 과거 추적 가능
- 나중에 실무에서 익숙해지기 위해

### 🖤 한 커밋에는 한 가지 문제만!

- 추적 가능하게 유지해주기
- 너무 많은 문제를 한 커밋에 담으면 추적하기 어렵다.

### 🖤 CLI에서 커밋 메시지 여러 줄로 작성하는 방법

<aside>
✅ **쌍따옴표를 닫지 말고 개행하며 작성 → 다 작성한 후에 쌍따옴표를 닫으면 작성 완료**

기본은 한 줄이다.

```bash
git commit -m "feat: 회원가입 기능 추가"
```

본문이 꼭 필요할 때만 개행해서 이유를 적는다.

```bash
git commit -m "fix: 주문 중복 체결 방지

동시 요청 시 잔액 검증이 통과되어 이중 체결되던 문제.
멱등성 키로 중복 요청을 차단함."
```

</aside>