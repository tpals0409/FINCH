새 스프린트를 엽니다. 아래 단계를 순서대로 수행하세요.

## 1단계: 컨텍스트 로딩

`~/.claude/projects/-Users-kimsemin-Desktop-FINCH/memory/sprint-window.md` 를 읽으세요.

> MEMORY.md 는 시스템이 자동 주입하고, git status/log 는 이미 제공됩니다. 별도 로딩 불필요.

## 2단계: 상태 검증

sprint-window.md 의 frontmatter `status` 를 확인하세요:
- `idle` → 정상. 3단계로 진행
- `active` → **경고**: "이전 스프린트가 정상 종료되지 않았습니다. `/sprint-close` 를 먼저 실행하거나, 이어서 작업할지 알려주세요." 출력 후 사용자 응답 대기
  - "이어서" → 3~4단계 스킵, 바로 **5단계(대시보드)** 출력. start_commit/제목 변경하지 않음
  - "/sprint-close 먼저" → 안내 후 종료
- 필드 없음 → idle 로 간주하고 진행

## 2.5단계: 스프린트 번호 교차 검증

`docs/adr/sprints/` 에서 가장 최신 ADR 의 번호를 확인하세요:
- `ls docs/adr/sprints/ | sort -V | tail -1` → `sprint-{M}.md` (파일이 없으면 M = 0)
- sprint-window.md [2] 의 번호 **N** 과 **M** 을 비교
- **|N − M| > 2** 이면 경고: "스프린트 번호 불일치: 메모리 Sprint {N} vs ADR Sprint {M}. sprint-window.md 가 미갱신 상태일 수 있습니다." 출력 후 사용자 응답 대기
- 차이 ≤ 2 → 정상

## 3단계: 스프린트 제목 결정

[2] 섹션의 제목을 아래 우선순위로 결정하세요:

1. **$ARGUMENTS 가 있으면** → 인자를 제목으로, [2] 의 제목/목표 갱신
2. **[2] 제목이 이미 있으면** (≠ "미정") → 유지
3. **"미정"이고 인자 없음** → 질문: "이번 스프린트 주제를 알려주세요. (미정으로 시작하려면 Enter)"

## 4단계: 스프린트 활성화

sprint-window.md 를 갱신하세요:
1. frontmatter `status` 를 `active` 로
2. [2] 의 `start_commit` 에 `git rev-parse --short HEAD` 기록
3. 3단계에서 제목이 정해졌으면 [2] 의 제목/목표 갱신

## 5단계: 대시보드 출력

```
## FINCH 대시보드

| 항목 | 상태 |
|------|------|
| 현재 스프린트 | Sprint {N} — {제목} ({idle → 신규 시작 / active → 이어서}) |
| 이전 스프린트 | Sprint {N-1} — {제목} (완료 ✅) |

### 잔여 항목
MEMORY.md "후속 처리 필요" 섹션의 미완료 항목만 나열

### 작업 안내
브랜치: git switch -c <type>/sprint-{N}-<설명>
파트 브리핑: backend/ · frontend/ · ai/ · infra/ 의 CLAUDE.md
```

## 5.5단계: 컨텍스트 합성

플랜 수립 전 아래를 확인하세요:
1. **이번 스프린트 하드 제약** (기술 부채·보안·CI): 최대 3개
2. **선결 리스크**: 작업 전 해소해야 할 미지의 사항. 기존 ADR 과 충돌하는지 포함
3. **관련 ADR**: `grep -rl "키워드" docs/adr/` 로 연관 번호 식별
4. **가정 명시**: 코드베이스 상태에 대한 전제. 틀리면 어떻게 대응할지

## 6단계: 플랜 모드 진입

대시보드 출력 후 **자동으로 플랜 모드에 진입**하세요.
- 제목/목표와 계획 작업을 기반으로 실행 계획을 세웁니다
- 코드베이스를 탐색해 계획의 실현 가능성을 검증합니다
- 계획이 확정되면 사용자에게 제시하고 승인을 받습니다

## 7단계: 작업 진행

플랜 승인 후 작업 브랜치를 만들고 계획된 작업을 순서대로 진행합니다.
