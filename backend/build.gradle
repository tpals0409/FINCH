plugins {
	// kotlin.plugin.spring(all-open) 이 없으면 Kotlin 클래스가 기본 final 이라 Spring 이
	// @Configuration·@Transactional 프록시를 만들지 못하고 기동 시점에 실패한다.
	// kotlin.plugin.jpa(no-arg) 는 엔티티의 기본 생성자를 만든다 — Hibernate 가 요구한다.
	// 플러그인 버전은 Boot 4.1.1 BOM 의 kotlin.version 과 같은 2.3.21 이다.
	// 갈리면 컴파일러가 만든 바이트코드와 런타임 stdlib 이 어긋난다.
	id 'org.jetbrains.kotlin.jvm' version '2.3.21'
	id 'org.jetbrains.kotlin.plugin.spring' version '2.3.21'
	id 'org.jetbrains.kotlin.plugin.jpa' version '2.3.21'
	id 'org.springframework.boot' version '4.1.1'
	id 'io.spring.dependency-management' version '1.1.7'
}

group = 'com.finch'
version = '0.0.1-SNAPSHOT'

kotlin {
	jvmToolchain(21)
	compilerOptions {
		// Spring 은 JSpecify 로 널 가능성을 표시한다. strict 로 두면 그 표시가 Kotlin 타입에 그대로 반영돼
		// 플랫폼 타입으로 새지 않는다 — 널 정보를 타입에 담는 것이 이 이관의 목적이므로 켠다.
		freeCompilerArgs.add('-Xjsr305=strict')
	}
}

repositories {
	mavenCentral()
}

dependencies {
	// jjwt 는 api·impl·jackson 세 모듈로 나뉜다. 버전이 서로 갈리면 런타임 NoSuchMethodError 가 나므로
	// BOM 으로 한곳에서 맞춘다. 올릴 때 이 줄만 고치면 된다.
	implementation platform('io.jsonwebtoken:jjwt-bom:0.13.0')

	implementation 'org.springframework.boot:spring-boot-starter-actuator'
	implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
	implementation 'org.springframework.boot:spring-boot-starter-data-redis'
	implementation 'org.springframework.boot:spring-boot-starter-flyway'
	implementation 'org.springframework.boot:spring-boot-starter-security'
	implementation 'org.springframework.boot:spring-boot-starter-validation'
	implementation 'org.springframework.boot:spring-boot-starter-webflux'
	implementation 'org.springframework.boot:spring-boot-starter-webmvc'
	implementation 'org.springframework.boot:spring-boot-starter-websocket'
	implementation 'io.jsonwebtoken:jjwt-api'
	implementation 'org.flywaydb:flyway-database-postgresql'
	// Spring 이 Kotlin 클래스의 주 생성자 파라미터 이름과 널 가능성을 읽는 데 쓴다. 없으면 기동하지 못한다.
	implementation 'org.jetbrains.kotlin:kotlin-reflect'
	// Java record 는 Jackson 이 정규 생성자를 스스로 찾지만 Kotlin data class 는 그렇지 않다.
	// 이 모듈이 주 생성자를 크리에이터로 잡아 준다 — 없으면 요청 본문 역직렬화가 깨진다.
	implementation 'tools.jackson.module:jackson-module-kotlin'
	runtimeOnly 'io.jsonwebtoken:jjwt-impl'
	runtimeOnly 'io.jsonwebtoken:jjwt-jackson'
	runtimeOnly 'io.micrometer:micrometer-registry-prometheus'
	runtimeOnly 'org.postgresql:postgresql'
	testImplementation 'org.springframework.boot:spring-boot-starter-actuator-test'
	testImplementation 'org.springframework.boot:spring-boot-starter-data-jpa-test'
	testImplementation 'org.springframework.boot:spring-boot-starter-data-redis-test'
	testImplementation 'org.springframework.boot:spring-boot-starter-flyway-test'
	testImplementation 'org.springframework.boot:spring-boot-starter-security-test'
	testImplementation 'org.springframework.boot:spring-boot-starter-validation-test'
	testImplementation 'org.springframework.boot:spring-boot-starter-webflux-test'
	testImplementation 'org.springframework.boot:spring-boot-starter-webmvc-test'
	testImplementation 'org.springframework.boot:spring-boot-starter-websocket-test'
	testImplementation 'org.springframework.boot:spring-boot-testcontainers'
	testImplementation 'org.testcontainers:testcontainers-junit-jupiter'
	testImplementation 'org.testcontainers:testcontainers-postgresql'
	testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
}

tasks.named('test') {
	useJUnitPlatform()
}
